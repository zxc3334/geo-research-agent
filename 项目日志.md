# GeoResearch Agent 项目日志

## 项目目标

将原 `deepresearch-agent` 改造成面向 GIS / 遥感研究问题的 evidence-aware 深度探索系统。第一阶段目标不是做全自动遥感计算平台，而是先跑通一个可展示、可解释、可逐步升级的项目副本。

## TODO

### M0：项目副本与环境

- [x] 确认原项目目录和目标项目目录
- [x] 复制原项目源码到新目录
- [x] 创建 Python 虚拟环境
- [x] 安装最小可运行依赖
- [x] 安装本地 editable package
- [x] 做一次基础导入/语法验证
- [x] 安装真实 Memory/RAG 依赖（CPU `torch`、`transformers`、`sentence-transformers`、`scikit-learn`）
- [ ] 可选：安装报告/数据分析依赖（`pandas`、`matplotlib`）
- [ ] 可选：安装评测/自进化依赖（`datasets`）

### M1：先跑通原项目主流程

- [x] 准备 `.env` / `.env.local`，配置可用 LLM 后端
- [x] 使用 mock tools 跑通一个最小 research query
- [x] 保存一份可展示的 baseline 输出报告
- [x] 记录当前原项目的已知问题和可改造点

### M2：GIS/遥感领域化 MVP

- [x] 梳理 GIS/遥感升级的 MVP 模块边界
- [x] 扩展 `TaskType`，区分文献检索、数据发现、方法设计、验证、合成
- [x] 改造 Planner prompt，使其生成 GIS/遥感研究 DAG
- [x] 改造 Summarizer 输出 GIS/遥感研究报告结构

### M2.5：AgentPool / Agent 架构补强

- [x] 抽出 `PromptBuilder`
- [x] 抽出 `ToolRegistry`
- [x] 抽出 `ToolCallingLoop`
- [x] 明确 AgentPool 池化的是绑定好 policy、tools、prompt builder、loop config、健康状态的 executor
- [x] 保证 `ToolCallingLoop` 的 messages / trajectory / token state 每次 `run()` 新建
- [x] 避免 AgentPool 中多个 agent 共享同一个 mutable policy 实例
- [x] 增加 agent health / metrics 基础状态

### M3：Evidence-aware 机制

- [x] 设计 evidence-aware 数据结构
- [x] 设计 `EvidenceStore`
- [x] 设计 Verified / Evidence-backed / Speculative / Rejected 分级
- [x] 设计验证失败后的 replan 反馈格式
- [x] 将 GIS/遥感专用工具输出升级为结构化 evidence

### M4：GIS/遥感工具

- [x] 设计 GIS/遥感专用工具接口
- [x] 实现 `dataset_registry_tool`
- [x] 实现 `method_registry_tool`
- [x] 实现 `geo_plan_validator_tool`
- [ ] 可选：实现 STAC 检索工具

### M5：Demo 与简历展示

- [ ] 准备第一个稳定 demo query
- [ ] 生成 demo 报告、日志和架构图
- [ ] 整理 README：项目目标、架构、运行方式、示例输出
- [ ] 整理简历 bullet points

### M5.1：Trace 可视化报告

- [x] 保留 `trace.jsonl` 作为机器可读原始事件流
- [x] 新增 `scripts/render_trace.py`，将 `trace.jsonl` 渲染为本地 HTML 报告
- [x] Trace HTML 展示 run summary、DAG、timeline/waterfall、tool calls、evidence table、LLM usage/cache hit
- [x] 在 demo 输出目录中生成 `trace_report.html`
- [x] 修正 `run_end` usage 汇总口径，包含最终 summarizer usage
- [x] 评估是否需要接入 Phoenix / Langfuse / LangSmith，作为后续可选项而非当前阻塞项

### M6：Provider / Model 架构重构

- [ ] 将 `VLLMPolicy` 重命名或抽象为更准确的 `OpenAICompatiblePolicy`
- [ ] 将 provider 配置、module-to-model 路由、policy 创建、policy 缓存拆分清楚
- [ ] 将 provider 配置迁移为 YAML 显式结构，env 仅保存 API key 等敏感信息
- [ ] 支持 `providers` 与 `modules` 两层配置：provider 描述 API 服务商，module 描述 planner/solver/summarizer 等模型使用策略
- [ ] 保持当前 DeepSeek / OpenAI-compatible 行为兼容，避免影响 M5 demo
- [ ] 为后续 Anthropic / Gemini / 本地 vLLM 留出 provider adapter 扩展点

## 执行记录

### 2026-05-23

- 已确认源项目目录：`D:\研究生\找实习\deep-research\deepresearch-agent`
- 已创建目标项目目录：`D:\研究生\找实习\geo-research-agent`
- 已复制源码到目标目录，排除了 `.git`、虚拟环境、缓存和历史输出目录。
- 已创建 Python 虚拟环境：`.venv`
- 当前 Python 版本：`Python 3.11.4`
- 第一次执行 `pip install -r requirements.txt` 失败，原因是 Windows 环境下 pip 使用 GBK 解码 `requirements.txt`，遇到 UTF-8 字符触发 `UnicodeDecodeError`。下一步改用 `python -X utf8 -m pip install -r requirements.txt`。
- 完整依赖安装进入 `transformers` 下载阶段后速度过慢，后续还会遇到 `torch` 等大包。为保证项目先可运行，新增 `requirements-minimal.txt`，先安装 MVP 运行所需依赖；完整依赖作为后续可选环境任务。
- 已安装最小可运行依赖：`openai`、`aiohttp`、`pyyaml`、`python-dotenv`、`numpy`、`beautifulsoup4` 及其传递依赖。
- 已执行 `pip install -e . --no-deps`，项目已作为本地 editable package 安装。
- 已通过 `compileall` 语法验证：`src` 和 `scripts` 编译通过。
- 已完成核心导入验证：`load_config`、`OrchestratorState`、`MockWebSearchTool`、`CalculatorTool`、`Embedder` 均可导入和运行。
- `Embedder` 当前使用 deterministic random fallback，原因是最小环境未安装 `sentence-transformers`。这是预期行为，后续需要高质量语义检索时再安装完整 ML 依赖。
- 已安装真实 Memory/RAG 依赖并完成版本验证：
  - `torch: 2.12.0+cpu`
  - `cuda_available: False`
  - `scikit-learn: 1.8.0`
  - `transformers: 5.9.0`
  - `sentence-transformers: 5.5.1`
- 已完成真实 embedding 模型验证：`sentence-transformers/all-MiniLM-L6-v2` 可加载并生成向量，真实语义 Memory/RAG 链路可用。
- 已创建 baseline 配置：`configs/baseline.yaml`
  - 所有主要 LLM 模块统一使用 `deepseek`
  - `tools.web_search.mock_mode=true`
  - `adversarial.enabled=false`
  - 降低并发、超时和 token 预算，便于 smoke test
- 已完成 baseline query：
  - Query：`简要分析遥感研究中使用 Landsat 进行城市热岛分析的一般流程`
  - 配置：`configs/baseline.yaml`
  - 输出目录：`outputs/baseline`
  - 运行日志：`outputs/baseline/run_20260525_183924.log`
  - 报告文件：`outputs/baseline/report_20260525_184233_简要分析遥感研究中使用_Landsat_.md`
  - 结果：DAG 生成成功，7 个子任务，3 个执行层，6/7 子任务成功，最终报告生成成功。
  - 真实 semantic memory 已参与运行，`all-MiniLM-L6-v2` 成功加载，并检测到若干 memory conflict。
- 当前 baseline 暴露的已知问题：
  - 使用 mock search 时，报告能跑通但 citation 可信度不足。
  - Planner 仍可能生成超过 baseline 预期的子任务数量，`max_sub_questions` 目前没有强约束。
  - 当前 success 判断仍偏执行状态，不判断 GIS/遥感方法是否被证据验证。
  - Memory conflict detection 已触发，但还没有 domain-aware 的冲突解释和处理。

### 2026-05-25

- 进入 M2：GIS/遥感领域化 MVP 设计。
- 已创建设计文档：`docs/geo_mvp_design.md`
- 已重写 `README.md`，将项目首页从原 `DeepResearch Agent` 叙事改为个人项目 `GeoResearch Agent`：
  - 明确项目定位：面向 GIS / 遥感研究问题的 evidence-aware DeepResearch Agent。
  - 记录当前 baseline 能力、MVP 改造目标、环境安装、密钥配置、baseline 运行方式。
  - 保留原框架来源和当前非核心模块说明，避免误导。
- M2 MVP 边界已确定：
  - 聚焦“遥感研究方案设计”，暂不做完整影像下载、GEE 自动执行、深度学习训练、QGIS 插件或完整 STAC 生产化接入。
  - 保留现有 Orchestrator 状态机，不重写主流程。
  - 第一轮代码改造采用兼容策略：保留旧 `SEARCH / ANALYZE / VERIFY`，新增 GIS/遥感 task types。
  - 新 task types 初期仍映射到现有 `ResearcherAgent`，先通过 Planner prompt、工具和 Summarizer 做领域化。
- 已确定 M2 代码改造顺序：
  1. 扩展 `TaskType`，保留旧类型兼容。
  2. 新增 `configs/geo_mvp.yaml`，沿用 baseline 的稳定配置。
  3. 改造 Planner prompt，生成 GIS/遥感任务类型。
  4. 改造 AgentPool，让新 task types 可执行。
  5. 改造 Summarizer prompt，输出 GIS/遥感报告结构和可信度分级占位。
  6. 跑 demo query，确认不会破坏 baseline。
- 记录一个后续架构改进点：当前 `AgentPool` 池化对象过轻，`ResearcherAgent.run()` 内部同时承担 prompt 构建、tool registry、tool-calling loop、运行状态、failure 判断等职责。后续建议将 Agent 重构为绑定好 `PromptBuilder`、`ToolRegistry`、`ToolCallingLoop`、`LoopConfig`、`Policy`、`Compressor`、`MemoryAdapter` 和健康状态的可复用执行器；但每次 `run()` 必须新建 loop runtime state，避免对象池复用导致 messages / trajectory / tool results 上下文污染。
- 当前适合建立 git 初始快照：M0/M1 已跑通，M2 设计文档已落地，但尚未开始核心代码改造。
- 已完成 M2 第一批核心代码改造：
  - 扩展 `TaskType`：保留 `search/analyze/verify` 兼容，同时新增 `literature/data_discovery/method_design/geo_validation/synthesis`。
  - 新增 `configs/geo_mvp.yaml`：沿用 DeepSeek + mock tools + 低并发 smoke test 配置，并设置 `planner.domain=geo_remote_sensing`。
  - 改造 `Planner`：支持 `domain` 参数；当 domain 为 `geo_remote_sensing` 时使用 GIS/遥感专用规划 prompt。
  - 改造 `core.runner`：从配置读取 `planner.domain` 并传入 `Planner`。
  - 改造 `AgentPool`：让 `literature/data_discovery/method_design/geo_validation` 路由到 `ResearcherAgent`，`synthesis` 路由到 `SummarizerAgent`。
  - 修复 `AgentPool` 回收类型推断：给 agent 记录 `pool_type_key`，避免旧实现把 `analyze/verify` 类型的 researcher 回收到 `search` 池。
  - 明确 `synthesis` 当前作为 Orchestrator 最终汇总阶段的保留类型，不让 Planner 在普通 DAG 中生成内部 synthesis 子任务。
- 已增加 GIS/遥感查询约束抽取：
  - 从用户 query 中抽取显式 `time_range` 和 `aoi`，写入 Planner prompt 的 `Extracted Query Constraints`。
  - 目标是降低 LLM planner 漂移，避免把“武汉”这类明确 AOI 泛化成 `selected AOI`。
  - 已验证样例：`2018-2024 年武汉...` 抽取为 `time_range=2018-2024`、`aoi=武汉`；`2020-2023年南京...` 抽取为 `南京`；`长江流域...` 抽取为 `长江流域`。
- 已完成验证：
  - `python -X utf8 -m compileall -q src scripts` 通过。
  - 使用 fake policy 验证新 task types 可解析为 DAG。
  - `configs/baseline.yaml` 和 `configs/geo_mvp.yaml` 均可初始化主流程对象。
- 已完成 M2 Summarizer 领域化：
  - `SummarizerAgent` 根据 `context["domain"]` 选择通用报告 prompt 或 GIS/遥感报告 prompt。
  - GIS/遥感报告结构固定包含：研究问题与核心结论、数据候选与适用性、方法链路设计、GIS/遥感验证清单、风险限制与替代方案、可执行 MVP 工作流、证据与引用、可信度分级。
  - 报告 prompt 要求区分 `Verified / Evidence-backed / Speculative / Rejected/Not supported`，并明确不能虚构数据集、波段、算法能力、空间分辨率、时间分辨率或验证数据。
- 已改造合成阶段的 AgentPool 使用方式：
  - Orchestrator 最终合成任务现在使用 `TaskType.SYNTHESIS`，不再伪装成 `ANALYZE`。
  - `AgentPool` 支持 `policy_factory_by_type`，当前 `synthesis` 绑定 `summarizer_policy`，普通研究子任务仍绑定 `solver_policy`。
  - 已验证：`SYNTHESIS` 创建 `SummarizerAgent + summarizer_policy`，`DATA_DISCOVERY` 创建 `ResearcherAgent + solver_policy`。
- 已完成 `geo_mvp.yaml` 端到端 smoke test：
  - Query：`如何研究 2018-2024 年武汉城市扩张对地表热环境的影响？`
  - 输出目录：`outputs/geo_mvp`
  - 运行日志：`outputs/geo_mvp/run_20260525_193635.log`
  - 报告文件：`outputs/geo_mvp/report_20260525_194006_如何研究_2018-2024_年武汉城市.md`
  - 结果：Planner 生成 4 个 GIS/遥感子任务，4 个执行层；3/4 子任务成功；最终报告合成成功，置信度 0.43。
  - 观察：mock search 下模型仍会补充大量常识性内容，真实可信度不足。下一阶段 M3 需要把 evidence-aware 数据结构、证据分级和验证失败反馈落到代码里。
- 进入 M2.5：AgentPool / Agent 架构补强。
- 已抽出 `src/agents/prompt_builder.py`：
  - `ResearchPromptBuilder` 负责 researcher 的 system prompt、direct-analysis system prompt、task prompt 和 non-searchable 判断。
  - `ResearcherAgent.run()` 不再直接拼接长 prompt。
- 已抽出 `src/agents/tool_registry.py`：
  - `ToolRegistry` 负责工具名称映射、OpenAI tool schema 导出和工具执行错误归一化。
  - Agent 持有 registry，loop 只通过 registry 执行工具。
- 已抽出 `src/agents/tool_calling_loop.py`：
  - `ToolLoopConfig` 固化 loop 参数，例如 `max_turns` 和强制总结阈值。
  - `ToolCallingLoop` 负责单次运行中的 `messages`、`trajectory`、`total_tokens`、tool calls、tool results 和终止判断。
  - 每次 `ResearcherAgent.run()` 都新建一个 `ToolCallingLoop`，避免 AgentPool 复用 agent 时污染上下文。
- 已简化 `ResearcherAgent`：
  - 现在它是池化 executor，持有 `policy / prompt_builder / tool_registry / loop_config / health / metrics`。
  - direct analysis 仍保留，但同步 policy 调用已改为 `asyncio.to_thread`，避免阻塞事件循环。
- 已补强 `BaseAgent`：
  - 新增 `AgentHealth`：`degraded / last_error / failure_count`。
  - 新增 `AgentMetrics`：`runs / total_tokens / tool_calls`。
  - 新增 `finalize_result()`，统一记录运行结果。
- 已补强 `AgentPool`：
  - 回收和复用时检查 `agent.health.degraded` 和 `policy.was_truncated`。
  - 保留 `pool_type_key`，避免不同 task type 的同类 Agent 被回收到错误池。
- 已修复 policy 共享问题：
  - `ModelRouter.create_backend(..., use_cache=False)` 支持创建非缓存 policy。
  - AgentPool 的 `solver` 和 `synthesis` policy factory 现在每次新建 policy，避免 tools / was_truncated 等 mutable 状态跨 agent 共享。
- M2.5 验证：
  - `python -X utf8 -m compileall -q src scripts` 通过。
  - 最小 fake policy + fake web_search 工具验证通过：`ResearcherAgent` 可通过新 `ToolCallingLoop` 完成工具调用并记录 metrics。
  - `configs/baseline.yaml` 和 `configs/geo_mvp.yaml` 模块初始化通过。
  - 端到端 smoke test 通过：
    - Query：`简要说明 Landsat 做城市热岛分析需要哪些数据和验证步骤`
    - 配置：`configs/geo_mvp.yaml`
    - 输出目录：`outputs/m25_smoke`
    - 报告文件：`outputs/m25_smoke/report_20260525_201723_简要说明_Landsat_做城市热岛分析.md`
    - 结果：Planner 生成 4 个 GIS/遥感子任务；3/4 子任务成功；最终报告合成成功，置信度 0.43。
- 按 code-review 结果完成 M2.5 稳定性修复：
  - `SummarizerAgent` 禁用 tools 时改为 `try/finally` 恢复 `policy.tools`，并使用 `asyncio.to_thread` 避免阻塞事件循环。
  - `ToolCallingLoop` 捕获非 `RuntimeError` 的 policy 异常并归一化为 `AgentResult(status=FAILED)`，保证 agent health/metrics 可记录失败。
  - `ModelRouter.create_backend(use_cache=False)` 不再提前计算 cache key，避免未来 unhashable kwargs 触发 `TypeError`。
  - 已验证：异常 policy 会返回 failed result；summarizer 异常后 tools 可恢复；`use_cache=False` 不会被 unhashable kwargs 卡住。
- 进入 M3：Evidence-aware 机制。
- 已新增 evidence-aware schema：
  - `EvidenceLevel`：`verified / evidence_backed / speculative / rejected`
  - `EvidenceItem`：包含 `claim / level / source / rationale / task_id / confidence / metadata`
  - `AgentResult.evidence_items`：子任务结果可携带结构化证据条目
  - `ResearchReport.evidence_summary`：最终报告可携带证据分级统计
- 已新增 `src/evidence/evidence_store.py`：
  - `EvidenceStore.annotate_result()`：给 `AgentResult` 自动补充 evidence items
  - `extract_sources()`：从 tool trajectory 中抽取 web search / arxiv sources
  - `classify()`：根据任务状态、tool evidence、mock source、task type、confidence 判断证据等级
  - `summarize()`：按 evidence level 汇总 claims
  - `build_replan_feedback()`：把 rejected / speculative claim 转成 replanner 可读反馈
- 已接入 Orchestrator：
  - collecting 阶段对每个结果执行 evidence annotation
  - 写入 SharedMemoryStore 时将 `evidence_items` 和 `evidence_level` 放入 memory metadata
  - memory 的 legacy `evidence_type` 由 evidence level 映射：`verified -> primary`，`evidence_backed -> secondary`，其余为 `inference`
  - synthesis context 中加入 `evidence_summary`
  - replan reason 中加入 rejected/speculative evidence feedback
- 已接入 Summarizer：
  - synthesis prompt 中加入全局 `Evidence Summary`
  - 每个 sub-task result 后附加 evidence level、source 和 rationale
  - 最终 `ResearchReport` 生成 `evidence_summary`
- 已更新报告格式化：
  - `_format_report()` 在元信息后输出 `证据分级统计`
- M3 轻量验证：
  - `python -X utf8 -m compileall -q src scripts` 通过。
  - mock source 会被标记为 `speculative`。
  - 真实 URL + `geo_validation` 成功结果会被标记为 `verified`。
  - Summarizer prompt 能看到 `Evidence Summary` 和 per-result evidence level。
- 进入 M4：GIS/遥感结构化工具。
- 已新增 `src/tools/geo_registry.py`：
  - `DatasetRegistryTool`：返回 Landsat、Sentinel-2、MODIS LST、ERA5-Land 的变量、分辨率、时间分辨率、所需波段、限制和官方来源。
  - `MethodRegistryTool`：返回 NDVI、NDBI、Landsat single-channel LST、GWR 的公式、输入、适用场景和限制。
  - `GeoPlanValidatorTool`：检查常见 GIS/遥感方案错误，例如 Sentinel-2 直接反演 LST、MODIS LST 直接当 30 m 使用、Landsat LST 条件、NDBI 混淆裸土等。
- 已接入工具注册：
  - `src/tools/__init__.py` 导出三个 GIS 工具。
  - `src/core/runner.py` 通过 `tools.geo_registry.enabled` 注册工具。
  - `configs/geo_mvp.yaml` 已启用 `tools.geo_registry.enabled=true`。
- 已更新 `ResearchPromptBuilder`：
  - system prompt 增加 `dataset_registry / method_registry / geo_plan_validator` 的工具说明。
  - task prompt 会根据 GIS/遥感关键词优先推荐结构化工具，而不是默认先走 web search。
- 已更新 `EvidenceStore`：
  - 可读取结构化工具返回的 `evidence_level` 和 `checks[].level`。
  - validator 的 `checks[]` 会被拆成 claim-level `EvidenceItem`，同一轮结果中可以同时保留 `rejected / verified / evidence_backed` 证据。
  - dataset/method registry 的 `official_sources` 会被抽取为 evidence source，避免结构化工具输出没有来源。
  - 写入 SharedMemoryStore 时优先选择 `verified / evidence_backed` claim 作为主记忆条目，避免把“被识别出的错误说法”当成下一轮检索主题。
  - code-review 后修复失败优先级：即使失败任务残留了结构化工具 trajectory，也会优先生成 `rejected` 证据，不会误标为 `verified`。
  - code-review 后修复空来源膨胀问题：空的 `url/title/snippet` 不再计入 source，避免无真实来源结果被误判为 source-backed。
- M4 轻量验证：
  - `python -X utf8 -m compileall -q src scripts` 通过。
  - `geo_mvp.yaml` 初始化后工具列表包含 `dataset_registry / method_registry / geo_plan_validator`。
  - `DatasetRegistryTool("Sentinel-2 LST")` 返回 Sentinel-2 数据集限制。
  - `MethodRegistryTool("NDBI formula")` 返回 NDBI 方法。
  - `GeoPlanValidatorTool("Use Sentinel-2 to directly retrieve LST and Landsat for LST")` 返回 `rejected` + `verified` checks。
  - EvidenceStore 能将结构化 validator 拆成多条证据：`Sentinel-2 can directly retrieve LST -> rejected`、`Landsat can support LST retrieval -> verified`、`NDBI can support built-up area screening -> evidence_backed`。
  - EvidenceStore 能从 `DatasetRegistryTool("Sentinel-2 LST")` 中抽取 ESA Sentinel-2 与 NASA MODIS LST 官方来源。
  - 失败任务即使带有 validator trajectory，也只会生成 `rejected` evidence item。
  - 空 source payload 会保持 `sources=[]`，分类结果为 `speculative`。
- M4 完整 demo 已跑通：
  - Query：`验证一个研究方案：用 Sentinel-2 直接反演 LST，并结合 NDBI 分析 2018-2024 年武汉城市扩张对热环境的影响，这个方案是否可行？需要哪些数据和修正？`
  - 配置：`configs/geo_mvp.yaml`
  - 输出目录：`outputs/m4_demo`
  - 运行日志：`outputs/m4_demo/run_20260526_100912.log`
  - 报告文件：`outputs/m4_demo/report_20260526_101301_验证一个研究方案_用_Sentinel-.md`
  - 结果：Planner 生成 5 个 GIS/遥感子任务，5 个执行层；4/5 子任务成功；最终报告合成成功，置信度 0.45。
  - 观察：LLM 能稳定调用 `geo_plan_validator`，并识别 Sentinel-2 不能直接反演 LST；当前报告中已有 `GeoResearch built-in validator` 证据来源。

### 2026-05-26

- 进入 M5.0：真实证据源接入准备。
- 已新增真实搜索配置：`configs/geo_real_search.yaml`
  - 保留 `configs/geo_mvp.yaml` 作为稳定 mock demo。
  - `geo_real_search.yaml` 将 `tools.web_search.mock_mode` 设置为 `false`。
  - 使用独立 memory DB：`data/geo_real_search_memory.db`，避免污染 mock demo 记忆。
  - 保留 GIS/遥感结构化工具：`dataset_registry / method_registry / geo_plan_validator`。
- 已验证 `geo_real_search.yaml` 可被 `load_config()` 正常加载：
  - `mock_mode=False`
  - `memory_db=data/geo_real_search_memory.db`
  - `domain=geo_remote_sensing`
- 已完成真实搜索 smoke test：
  - `.env.local` 可被加载，`SEARCH_BACKEND=bocha`，`BOCHA_API_KEY` 已检测到但未打印明文。
  - `configs/geo_real_search.yaml` 会创建真实 `WebSearchTool`，不是 `MockWebSearchTool`。
  - 直接调用 `WebSearchTool("Sentinel-2 MSI no thermal infrared band official")` 成功返回 3 条结果。
  - 观察：普通 Bocha query 会返回 CSDN / ResearchGate 等结果，官方来源优先级不足；后续需要加入 official-domain search 或 Tavily/Bocha domain filter。
- 已完成两次真实搜索端到端测试：
  - 完整 query：3/4 子任务成功，最终合成阶段 DeepSeek `Connection error`。
  - 短 query：5/5 子任务成功，但最终合成阶段 DeepSeek `Request timed out`。
  - 结论：搜索 key 和真实搜索工具链路可用；当前主要问题在最终合成 LLM 的可靠性和 prompt 长度控制。
- 已修复真实 demo 暴露的可靠性问题：
  - `SummarizerAgent` 现在会识别 policy 层包装的 `Error: Connection error` / `Error: Request timed out`，不再把错误文本当作成功报告。
  - `ToolCallingLoop` 增加英文网络/超时错误识别，避免研究子任务把 provider error 当作成功输出。
  - `SummarizerAgent` 会截断每个子任务输出和 evidence 条目，降低真实搜索下最终合成 prompt 过长导致的失败概率。
  - `Orchestrator` 新增 deterministic fallback synthesis：如果最终合成 LLM 失败，会基于已有子任务结果和 evidence items 生成降级研究报告，而不是只输出错误字符串。
- 已补强 M5.0 工具链：
  - 新增 `official_source_search`，用于优先检索 GIS/遥感官方来源。
  - 该工具底层复用 `WebSearchTool`，同时用本地 registry 中的官方 URL 作为稳定种子，再尝试联网补充官方域名结果。
  - 对联网结果增加 URL 域名硬过滤，避免 Bocha 对 `site:` 约束不严格时混入 CSDN / 商品页等非官方结果。
  - `geo_real_search.yaml` 下会注册 `official_source_search`；mock 配置下仍保持稳定，不注册真实搜索工具。
- 已补强工具调用可观测性：
  - `ToolCallingLoop` 会在日志中打印 `[ToolCall] task / turn / tool / source / total / urls`。
  - `ResearchReport` 新增 `tool_trace` 字段。
  - `_format_report()` 会输出 `工具调用摘要`，展示每个 task 调用了哪些工具、返回了多少 URL、关键 URL 是什么。
- 关于搜索结果压缩的判断：
  - 需要压缩。真实搜索轮次多、结果多时，会同时影响 tool loop 上下文、summarizer prompt 和最终 LLM 稳定性。
  - 当前已有 `ContextCompressor`，但主要用于 planner/replan 的 memory context；不会自动压缩每轮 tool result。
  - 已改为动态 compact：工具返回结果和 summarizer 输入默认不压缩，只有估算上下文超过 70% budget 时才触发。
  - compact 策略采用静态 head/tail 70/30 中间截断，不使用 embedding，不使用 LLM 摘要。
  - compact 后必须显式标记 `[compact]`，并记录被省略字符数。
  - 失败、错误、异常、超时、API key 等信息不压缩，避免隐藏关键错误原因。
  - `ToolCallingLoop` 在追加 tool message 前检查 projected context；超过阈值才压缩长工具结果。
  - `SummarizerAgent` 先构造完整 synthesis prompt；超过阈值后才对成功子任务 output 和 evidence 列表做 compact。
  - 已验证：小 prompt 不 compact；长成功输出 compact；长错误输出不 compact 且保留完整错误前缀。
- 已将 compact 参数配置化：
  - `configs/geo_mvp.yaml` 和 `configs/geo_real_search.yaml` 均新增 `agents.researcher.tool_loop` 与 `agents.summarizer.compact` 配置段。
  - researcher tool loop 默认 `context_budget_tokens=12000`。
  - summarizer compact 默认 `context_budget_tokens=24000`，因为最终合成阶段需要容纳多个子任务结果与 evidence。
  - `AgentPool` 会把 YAML 中的 agent 配置注入 `ResearcherAgent` / `SummarizerAgent`。
  - 已验证：`geo_real_search.yaml` 初始化后 researcher budget=12000，summarizer budget=24000。
- 已新增本地 JSONL trace：
  - 新增 `src/observability/trace_recorder.py`，输出 append-only JSONL 事件流。
  - `scripts/run_single.py` 会在输出目录创建 `trace.jsonl`，并通过 config 注入 runner。
  - 记录事件包括：`modules_init_start/end`、`research_start/end`、`run_start/end`、`state_transition`、`task_planned`、`task_start/end`、`tool_call`、`tool_result`、`compact`、`evidence_item`、`synthesis_end`。
  - trace 会过滤敏感字段，例如 `api_key / Authorization / token / secret / password`。
  - 已验证：trace recorder 可写入 JSONL；AgentPool/Orchestrator 初始化可注入 recorder；tool loop 会写入 `tool_call/tool_result/compact` 事件。
- 已补充 JSONL trace 的 LLM usage 归一化：
  - 新增 `src/observability/usage.py`，将 OpenAI-compatible、AI SDK 风格、Anthropic 风格 usage 统一为 `input_tokens / output_tokens / cache_read_tokens / cache_write_tokens / total_tokens / cache_hit_rate`。
  - OpenAI / AI SDK 风格中 cached input 会从 input tokens 中扣除；Anthropic 原生 `cache_read_input_tokens` 不重复扣除。
  - `VLLMPolicy` 会把 provider 原始 usage 归一化后放入 response。
  - `ToolCallingLoop` 和 `SummarizerAgent` 的 `llm_call` trace 事件会记录 usage；`AgentResult.token_usage` 优先使用归一化后的 `total_tokens`。
  - 修复 `TraceRecorder` 敏感字段过滤过宽的问题：不再误删 `input_tokens / output_tokens / total_tokens`，但仍过滤 `api_key / Authorization / access_token / secret / password`。
  - 已验证：`compileall` 通过；OpenAI/AI SDK/Anthropic/object/None usage 归一化测试通过；researcher tool loop 和 summarizer 都能在 JSONL 中写入完整 usage。
- code-review 后补充修复：
  - `OfficialSourceSearchTool` 的 URL allowlist 支持 `developers.google.com/earth-engine` 这类“域名 + 路径”规则，避免误过滤 Google Earth Engine 官方文档。
  - `TraceRecorder` 支持纯文件名路径，例如 `trace.jsonl`，不会因为目录名为空而初始化失败。
- 已新增后续待办：
  - `M5.1：Trace 可视化报告`：把密集 JSONL 转换为本地 HTML trace viewer，便于展示 DAG、timeline、tool calls、evidence 和 usage。
  - `M6：Provider / Model 架构重构`：整理当前 `ModelRouter + VLLMPolicy` 的多 provider 胶水代码，将 provider 配置、module 路由和 policy adapter 解耦。
- 优先级判断：先做 M5.1，再做 M6。原因是当前真实 evidence demo 已经跑通，最大展示短板是 `trace.jsonl` 不适合人读；Trace Viewer 能直接提升简历和面试展示效果。Provider/model 重构属于架构质量提升，但短期不会显著改善 demo 可见价值，且改动面更大，适合在 M5 展示链路稳定后推进。
- 已完成 M5.1 Trace 可视化报告：
  - 新增 `scripts/render_trace.py`，读取 `trace.jsonl` 并生成离线 `trace_report.html`。
  - HTML 视图包含：Run summary、Task DAG、Execution Timeline、Tool Calls、Evidence Items、LLM Usage By Call、Usage By Task、Event Stream。
  - 设计参考 `ui-ux-pro-max` 的高优先级规则：数据密集 dashboard、清晰层级、可键盘切换 tabs、响应式布局、足够对比度、避免纯装饰元素。
  - `scripts/run_single.py` 在保存 Markdown 报告后会自动尝试生成 `trace_report.html`；生成失败只记录 warning，不阻断主流程。
  - 修正 `Orchestrator.run_end` usage 汇总口径，纳入最终 summarizer 的 usage。
  - 已用 `outputs/m5_real_search_demo/trace.jsonl` 验证生成：`outputs/m5_real_search_demo/trace_report.html`。
  - 已验证：`compileall` 通过；`render_trace.py` 可独立运行；`run_single.py` 可导入；HTML 中包含 DAG、timeline、tool、evidence、usage 关键 section。
- 根据 Trace Viewer 暴露的问题，补强 registry 证据来源策略：
  - 内置 `dataset_registry / method_registry / geo_plan_validator` 不再直接输出 `verified` 作为正向证据。
  - `EvidenceStore` 新增 provenance cap：本地 registry/validator 的正向结论最多作为 `speculative`；带官方 URL 的 curated registry 记录最多作为 `evidence_backed`；只有外部检索/验证链路才可进入 `verified`。
  - `rejected` 仍然保留为强约束反馈，用于阻止明显错误方案继续进入总结，例如 Sentinel-2 直接反演 LST。
  - evidence trace 事件新增 `source_type` 和 `metadata`，Trace Viewer 的 Evidence 表新增 Source Type 列，便于区分 `registry_heuristic`、`registry_curated_with_official_url`、`official_search` 等来源。
- 进入 M6：Provider / Model 架构重构。
  - 新增 `src/models/model_factory.py`，将 LLM 配置拆成三层：`provider`（API 服务商与密钥）、`profile`（模型与采样参数）、`module_profiles`（planner/solver/summarizer 等模块路由）。
  - `LLMModelFactory` 支持新配置，同时兼容旧的 `backend / backend_sampling / backend_mapping`，避免一次性破坏 baseline。
  - `ModelRouter` 改为兼容 facade，新代码优先使用 `LLMModelFactory`，旧模块仍可调用 `ModelRouter.create_backend()`。
  - `initialize_modules()` 改为通过 `LLMModelFactory` 创建 planner、solver、summarizer、compressor 等 policy，并把解析后的 provider/profile 信息写入日志。
  - `configs/geo_mvp.yaml` 和 `configs/geo_real_search.yaml` 已迁移到新配置格式。
  - `README.md` 新增 Provider / Model 配置说明，明确 provider、profile、module profile 的职责边界。
  - 已验证：`compileall` 通过；新旧 YAML 配置均可解析；`geo_mvp.yaml` 可完成模块初始化；`ModelRouter.create_backend("vllm")` 兼容入口可创建 policy。
  - code-review 检查点：未发现 P0/P1 问题；日志输出只包含 provider/profile/model/base_url 等非密钥信息，cache key 已排除 `api_key`。

## 当前阶段

M6 已完成第一轮：Provider / Model 架构已重构为 `providers / profiles / module_profiles` 三层配置，旧 `backend / backend_sampling / backend_mapping` 仍兼容。下一步可选择重跑真实 evidence demo，确认新 model factory 在端到端链路中稳定工作。
