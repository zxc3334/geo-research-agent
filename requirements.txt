# =============================================================================
# DeepResearch Agent — 完整依赖清单
# =============================================================================
# 安装方式：
#   pip install -r requirements.txt              # 仅核心依赖
#   pip install -r requirements.txt[train]       # 核心 + 训练依赖
#   pip install -r requirements.txt[serve]       # 核心 + vLLM 推理服务
#   pip install -r requirements.txt[dev]         # 核心 + 开发工具
#   pip install -r requirements.txt[all]         # 全部依赖
#
# 注意：
#   1. verl 框架为项目一/项目二本地依赖，不在 PyPI 上，
#      需通过 PYTHONPATH 引入或使用 pip install -e . 安装。
#   2. 若使用本地 vLLM 服务，需确保 CUDA 环境正确配置。
# =============================================================================

# ---------------------------------------------------------------------------
# 核心依赖（必须安装）
# ---------------------------------------------------------------------------

# LLM 客户端：调用 DeepSeek / OpenAI / vLLM 等 OpenAI 兼容 API
openai>=1.0

# HuggingFace 生态：Tokenizer、模型加载
transformers>=4.40

# 深度学习框架
# 注意：若使用 CUDA，请根据 CUDA 版本安装对应 torch
# 例如：pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0

# 科学计算
numpy>=1.24

# 异步 HTTP 客户端（工具层搜索、ArXiv API 调用）
aiohttp>=3.8

# YAML 配置解析
pyyaml>=6.0

# .env 环境变量管理（多后端 API Key 配置）
python-dotenv>=1.0

# LangSmith 追踪与可观测性（Agent 流程可视化，可选但强烈推荐）
langsmith>=0.1.0

# 文本向量化（M4 记忆嵌入 + M3 相关性过滤）
sentence-transformers>=2.5

# 图算法（M3 TextRank 关键句提取）
networkx>=3.0

# 机器学习工具（M3 相似度计算、评测指标）
scikit-learn>=1.3

# 进度条
tqdm>=4.65

# HuggingFace 数据集（评测数据加载）
datasets>=2.14

# 数据处理
pandas>=2.0

# 可视化（消融实验图表、评测报告）
matplotlib>=3.7

# ---------------------------------------------------------------------------
# 可选依赖：自进化训练（M6 GRPO 在线训练）
# 安装命令：pip install -r requirements.txt[train]
# ---------------------------------------------------------------------------
# peft>=0.11.0          # LoRA 微调
# accelerate>=0.30.0    # HuggingFace 分布式训练
# deepspeed>=0.14.0     # 大模型分布式训练
# wandb                 # 实验跟踪与可视化

# ---------------------------------------------------------------------------
# 可选依赖：vLLM 推理服务（本地部署大模型）
# 安装命令：pip install -r requirements.txt[serve]
# ---------------------------------------------------------------------------
# vllm>=0.4.0           # 高性能 LLM 推理引擎

# ---------------------------------------------------------------------------
# 可选依赖：真实搜索工具
# ---------------------------------------------------------------------------
# google-search-results  # SerpAPI 官方 Python SDK（WebSearchTool 真实模式）
# duckduckgo-search      # DuckDuckGo 搜索（免费，无需 API Key）

# ---------------------------------------------------------------------------
# 可选依赖：网页阅读与文档解析（BrowserTool / FileReaderTool）
# ---------------------------------------------------------------------------
beautifulsoup4>=4.12    # HTML 解析（BrowserTool 正文提取）
# pdfplumber>=0.10      # PDF 文本提取（推荐，表格保留更好）
# PyPDF2>=3.0           # PDF 文本提取（降级方案）
# python-docx>=1.1      # Word 文档读取

# ---------------------------------------------------------------------------
# 开发依赖（代码质量、测试）
# 安装命令：pip install -r requirements.txt[dev]
# ---------------------------------------------------------------------------
# pytest>=7.0
# pytest-asyncio
# black
# isort
# mypy
# flake8

# ---------------------------------------------------------------------------
# 本地依赖说明（非 PyPI 包）
# ---------------------------------------------------------------------------
# veRL 框架：项目一的核心训练框架，位于 /workspace/deep_research_agent/verl/
#   使用方式：export PYTHONPATH="/workspace/deep_research_agent:$PYTHONPATH"
#   或：cd /workspace/deep_research_agent/verl && pip install -e .
#
# tau-bench（项目一评测环境）：
#   安装方式：pip install tau-bench
